apiVersion: v1
kind: Service
metadata:
  name: {{ include "gatewise.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "gatewise.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "gatewise.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
